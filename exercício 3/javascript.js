'use strict'
let numero = prompt('Insira um número:')
let antecessor = (parseInt(numero) - 1);
let sucessor = (parseInt(numero) + 1);
    alert (`O antecessor do seu novo número é: ${antecessor}`);
    alert (`O sucessor do seu novo número é: ${sucessor}`);
